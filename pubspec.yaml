name: flutte
description: Budgeting app written in Flutter

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  shared_preferences: ^0.5.3
  page_view_indicator: ^2.0.0
  charts_flutter: ^0.6.0
  sqflite: ^1.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true